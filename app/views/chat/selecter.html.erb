<ul class="user-list">
  <% @users.each do |user| %>
    <li>
      <%= image_for(user) %>
      <%= link_to user.name, user %> |
      <%= link_to "チャット", chat_index_path(user_id_to:user.id)%>
    </li>
  <% end %>
</ul>